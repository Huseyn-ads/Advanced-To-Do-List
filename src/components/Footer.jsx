import React from 'react'

function Footer() {
  return (
    <footer className='footer'>
      <p>Technologies and React cocepts used: React, JSX, props, useState, component composition, conditional rendering, array methods(map, filter), event handling.</p>
    </footer>
  )
}

export default Footer